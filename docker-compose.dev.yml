services:
  aurora-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: aurora-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
    command: npm run dev
    networks:
      - aurora-network

networks:
  aurora-network:
    driver: bridge
